[
    {
        "id": "01-profile-get",
        "prompt": "Write a function returning the named InstagramProfile model that gets username northstar_forge_test with 24-hour freshness.",
        "required": ["from '@openhandle/sdk'", "InstagramProfile", ".instagram.profile(", ".get(", "freshness: '24h'"]
    },
    {
        "id": "02-explicit-references",
        "prompt": "Select a TikTok profile by opaque platform ID and an Instagram post by URL, then fetch both resources.",
        "required": ["from '@openhandle/sdk'", ".tiktok.profile({ id:", ".instagram.post({ url:", ".get("]
    },
    {
        "id": "03-pagination",
        "prompt": "Collect post IDs from every page of an Instagram profile's posts without constructing cursors manually. Use the named page type in the implementation.",
        "required": ["from '@openhandle/sdk'", "InstagramProfilePostsPage", ".posts.list(", "page.data", "page.next()"]
    },
    {
        "id": "04-nested-replies",
        "prompt": "List replies for a known Instagram post ID and comment ID using the nested resource graph.",
        "required": ["from '@openhandle/sdk'", ".instagram.post(", ".comment(", ".replies.list("]
    },
    {
        "id": "05-search",
        "prompt": "Search TikTok posts for synthetic with 24-hour freshness using the named options type.",
        "required": ["from '@openhandle/sdk'", "TikTokSearchPostsOptions", ".tiktok.search.posts.list(", "q: 'synthetic'", "freshness: '24h'"]
    },
    {
        "id": "06-url-fetch",
        "prompt": "Fetch the resource represented by an Instagram post URL when the caller does not know its resource type.",
        "required": ["from '@openhandle/sdk'", ".fetch(", "https://www.instagram.com/p/"]
    },
    {
        "id": "07-errors",
        "prompt": "Handle an SDK failure by returning its code, request ID, and retryable flag. Do not branch on the message.",
        "required": ["from '@openhandle/sdk'", "instanceof OpenHandleError", ".code", ".requestId", ".retryable"],
        "forbidden": [".message ===", ".message =="]
    },
    {
        "id": "08-request-controls",
        "prompt": "Get a Twitter profile with retries disabled, a five-second timeout, and a caller-provided AbortSignal.",
        "required": ["from '@openhandle/sdk'", ".twitter.profile(", "maxRetries: 0", "timeoutMs: 5_000", "signal"]
    },
    {
        "id": "09-billing-metadata",
        "prompt": "Get an Instagram profile using the named response type and return its authoritative cost, environment, and request ID metadata.",
        "required": ["from '@openhandle/sdk'", "InstagramProfileResponse", ".billing.cost", ".billing.environment", ".requestId"]
    },
    {
        "id": "10-key-selects-environment",
        "prompt": "Create a reusable client from a caller-provided Test or Live API key without adding a separate environment option.",
        "required": ["from '@openhandle/sdk'", "new OpenHandle({ apiKey })"],
        "forbidden": ["environment:", "testMode:", "liveMode:"]
    }
]
